# Copy to .env.local for local dev. On Vercel, set these in the project's
# Environment Variables settings instead.

# Project Settings -> API in the Supabase dashboard. Use the anon (public)
# key — never the service_role key, which stays backend-only.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
